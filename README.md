# CXR Analysis

This repository contains source code for generating decision tree visualizations to explain predictions of chest X-Ray classification algorithms.

The visualizations are already generated and stored in `plots`.

For now, only demographic features have been used (as seen in `config.json`).

The decision treee classifiers were trained on a single binary target (`Label_0`, `Label_1`, ...).